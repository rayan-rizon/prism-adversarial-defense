\documentclass{article}

% ── NeurIPS 2026 style ─────────────────────────────────────────────────────
% Available track options (uncomment exactly ONE for submission):
%   \usepackage{neurips_2026}                          % default = main track, double-blind, with line numbers
%   \usepackage[main]{neurips_2026}                    % main track explicitly
%   \usepackage[position]{neurips_2026}                % position-paper track
%   \usepackage[eandd]{neurips_2026}                   % evaluations & datasets track
%   \usepackage[creativeai]{neurips_2026}              % creative AI track
%   \usepackage[sglblindworkshop]{neurips_2026}        % single-blind workshop
%   \usepackage[dblblindworkshop]{neurips_2026}        % double-blind workshop
%
% Camera-ready: add `final` (e.g. `\usepackage[main, final]{neurips_2026}`).
% Build mode — toggle ONE line for the target submission:
%   - For arXiv / preprint with named author block, no line numbers:
\usepackage[preprint]{neurips_2026}
%   - For NeurIPS main-track double-blind submission (anonymises author,
%     adds line numbers): comment the line above and uncomment this:
% \usepackage[main]{neurips_2026}
%   - For camera-ready (named author, no line numbers, final styling):
% \usepackage[main, final]{neurips_2026}

% ── Required NeurIPS encoding / typography packages ────────────────────────
\usepackage[utf8]{inputenc} % allow utf-8 input
\usepackage[T1]{fontenc}    % use 8-bit T1 fonts
% Latin Modern (Type 1 outlines). The NeurIPS style defaults to Times (ptm),
% but every Times math package available here injects a Type 3 font; lmodern
% keeps the build Type-3-free.
\usepackage{lmodern}
\usepackage{cmap}           % searchable/copyable PDF text
\input{glyphtounicode}
\pdfgentounicode=1
\usepackage{hyperref}       % hyperlinks
\usepackage{url}            % simple URL typesetting
\usepackage{booktabs}       % professional-quality tables
\usepackage{amsfonts}       % blackboard math symbols
\usepackage{nicefrac}       % compact symbols for 1/2, etc.
\usepackage{microtype}      % microtypography
\usepackage{xcolor}         % colors

% ── Additional packages used by this paper ─────────────────────────────────
\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{subcaption}
\usepackage{float}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{xspace}
\usepackage{enumitem}

% ── Macros ─────────────────────────────────────────────────────────────────
\newcommand{\prism}{\textsc{PRISM}\xspace}
\newcommand{\tamm}{\textsc{TAMM}\xspace}
\newcommand{\cadg}{\textsc{CADG}\xspace}
\newcommand{\sacd}{\textsc{SACD}\xspace}
\newcommand{\tamsh}{\textsc{TAMSH}\xspace}

% Theorem environments
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{corollary}[theorem]{Corollary}

% ── Title ──────────────────────────────────────────────────────────────────
\title{\textsc{PRISM}: Predictive Runtime Immune System with Manifold Monitoring for
       Architecture-Agnostic Adversarial Defense}

% For anonymous submission (double-blind), keep author block empty / anonymized.
% For preprint / camera-ready, fill in real authors per neurips_2026.tex template.
% Author block. Visible under [preprint] and [main, final]; hidden under
% bare [main] (NeurIPS double-blind review anonymization).
\author{%
  Rayan Rizon\\
  Independent Researcher\\
  \texttt{rayan.rizon@gmail.com}\\
}

\begin{document}

\maketitle

\begin{abstract}
  \input{sections/abstract}
\end{abstract}

\input{sections/intro}
\input{sections/related}
\input{sections/method}
\input{sections/experiments}
\input{sections/conclusion}

% ── Acknowledgments (omit in anonymized submission) ────────────────────────
% \begin{ack}
%   Funding disclosure and competing-interest declarations go here. Required
%   for the camera-ready version. Hidden in the anonymized submission via
%   the `ack` environment provided by neurips_2026.sty.
% \end{ack}

% ── References ─────────────────────────────────────────────────────────────
\bibliography{prism}
\bibliographystyle{plainnat}

% ── Appendix ───────────────────────────────────────────────────────────────
\appendix
\input{sections/appendix}

% ── NeurIPS Paper Checklist (required, does not count toward page limit) ───
\newpage
\input{checklist}

\end{document}
