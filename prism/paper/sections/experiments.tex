\section{Experiments}
\label{sec:experiments}

We evaluate \prism on CIFAR-10 against white-box, black-box,
optimization-based, and adaptive attacks, followed by per-channel ablation,
matched-FPR baseline comparison, campaign-detection latency, and L3
recovery. To assess cross-architecture and cross-dataset generalization,
we additionally report the same detection and baseline protocol on
CIFAR-10/WRN-28-10 and CIFAR-100/ResNet-18
(\S\ref{sec:multi_backbone}, Table~\ref{tab:main_multi}); a full per-table
CIFAR-100 set (adaptive PGD, ablation, recovery, campaign, latency) is
deferred to Appendix~\ref{app:cifar100}. We also include a
CIFAR-10/ViT-B/16 transformer-transfer run for FGSM, PGD-50, and Square
only; ViT latency, CW-L2, AutoAttack, and adaptive PGD are not claimed.

% ── Setup ──────────────────────────────────────────────────────────────────
\subsection{Experimental Setup}
\label{sec:setup}

\paragraph{Base classifier.}
The main CIFAR-10 results use a CIFAR-native ResNet-18 \cite{he2016deep}
trained from scratch. For cross-architecture validation we additionally
train a WRN-28-10 \cite{zagoruyko2016wide} on CIFAR-10; for cross-dataset
validation we use ResNet-18 on CIFAR-100. \prism monitors activations at
$L=3$ depth slices weighted 0.30, 0.30, 0.40 (shallow-to-deep). For the
ResNet-18 backbones (CIFAR-10 and CIFAR-100) these are \texttt{layer2},
\texttt{layer3}, \texttt{layer4}; for the WRN-28-10 backbone (which has
three residual stages, not four) the analogous depth slice is
\texttt{layer1}, \texttt{layer2}, \texttt{layer3} with the same
0.30/0.30/0.40 weights, preserving the shallow-to-deep emphasis.
For these CNN settings, all inputs remain native $3\times 32\times 32$
images with dataset-specific CIFAR normalization; no ImageNet pretraining
or 224x224 upsampling is used. For the transformer-transfer run, we
fine-tune torchvision ViT-B/16 \cite{dosovitskiy2021image} from
ImageNet \cite{deng2009imagenet} at $224\times224$ on CIFAR-10 and
monitor \texttt{encoder\_layer\_3},
\texttt{encoder\_layer\_7}, and \texttt{encoder\_layer\_11} with the same
0.30/0.30/0.40 weights. The ViT backbone records test accuracy $0.9832$
and passes an independent $n{=}1000$ verification gate at $0.9860$.

\paragraph{PRISM calibration.}
We calibrate thresholds using $n_\text{cal}=2{,}000$ held-out clean images
(split conformal: $n_\text{cal}$ for threshold fitting,
$n_\text{val}=1{,}000$ for coverage verification),
with $\alpha \in \{0.10, 0.03, 0.005\}$.
Thresholds and validation FPRs are generated by the artifact pipeline and
reported from the resulting JSON files rather than manually transcribed.

\paragraph{Attacks.}
We evaluate the following attack classes, with exact per-setting scope
reported in Table~\ref{tab:main_multi} and Appendix~\ref{app:cifar100}:
\begin{itemize}[leftmargin=*,noitemsep]
  \item \textbf{FGSM} \cite{goodfellow2015explaining}: single-step gradient attack, $\varepsilon=8/255$ ($\ell_\infty$).
  \item \textbf{PGD} \cite{madry2017towards}: projected gradient descent, $\varepsilon=8/255$, step size $\alpha=\varepsilon/4$ ($\ell_\infty$). The CIFAR-10/ResNet-18 main table uses PGD-40 (40 steps, 1 restart, the ablation-pipeline config); WRN-28-10 and CIFAR-100 use the RobustBench-standard PGD-50 (50 steps, 10 restarts). Appendix~\ref{app:audit} verifies CIFAR-10 detection is invariant to PGD-50$\times$10 ($0.991$ vs.\ $0.987$).
  \item \textbf{CW-L2} \cite{carlini2017adversarial}: optimization-based attack, canonical configuration with 100 iterations and 9 binary-search steps, matching Table~\ref{tab:main}. A weakened-CW run (5 iterations, 5 binary-search steps) is reported in Appendix~\ref{app:weak_cw} for detector-fidelity analysis only.
  \item \textbf{Square Attack} \cite{andriushchenko2020square}: black-box score-based attack, 5000 queries, $\varepsilon=8/255$ ($\ell_\infty$).
  \item \textbf{AutoAttack} \cite{croce2020reliable}: standard ensemble evaluation.
  \item \textbf{Adaptive PGD}: on CIFAR-10/ResNet-18, ensemble-complete BPDA-style attack with activation matching, side-channel surrogates, $\lambda$ sweep, 100 steps, 10 restarts, and EOT verification metadata. WRN and CIFAR-100 adaptive rows are appendix stress probes with narrower coverage.
\end{itemize}
The ViT-B/16 transfer run is intentionally narrower: FGSM, PGD-50
(50 steps, 10 restarts), and Square (5000 queries), each at
$\varepsilon=8/255$ over the same 5 seeds and $n{=}1000$/seed.
All attacks are implemented via Adversarial Robustness Toolbox (ART) v1.20 
\cite{nicolae2018adversarial} or native PyTorch implementations in pixel
$[0,1]$ space; \prism receives CIFAR-normalized inputs after adversarial generation.

\paragraph{Baselines.}
We reproduce four matched-FPR baselines on the same splits:
\textbf{LID} \cite{ma2018characterizing}, \textbf{Mahalanobis}
\cite{lee2018simple}, \textbf{ODIN} \cite{liang2018enhancing}, and
\textbf{Energy} \cite{liu2020energy}.

\subsection{Threat Model and Coverage}
\label{sec:threat_model}
Table~\ref{tab:threat_model} organises the evaluation by adversary capability
and identity model, mapping each tier to the experiment that measures it and the
residual it leaves. Two axes matter: how much of \prism the attacker can see
(classifier only, a differentiable detector surrogate, or the exact deployed
score $S(x)$), and the granularity at which the campaign monitor resets (per
image, per session, or per account). \prism's single-shot adaptive robustness is
deliberately not overclaimed---a surrogate-free attack on the exact score drives
the per-image tier to $0.16$ (\S\ref{sec:direct_score})---and the single-session
campaign monitor is what restores the full-system miss to ${\le}0.03$
(\S\ref{sec:campaign_robust}); per-account identity rotation (Sybil) is the
explicit out-of-scope boundary of the C3 claim, addressed by orthogonal
account-level rate-limiting rather than by the detector.

\begin{table}[h]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{p{0.31\linewidth}p{0.14\linewidth}p{0.20\linewidth}p{0.23\linewidth}}
\toprule
Adversary (knowledge) & Identity model & Measured by & Result (CIFAR-10/ResNet-18) \\
\midrule
Black-box, score-based (Square) & per-image & Table~\ref{tab:main} & TPR $0.886$ @ FPR $0.074$ \\
White-box classifier, detector-blind (FGSM/PGD/CW/AA) & per-image & Table~\ref{tab:main} & TPR $0.88$--$1.00$ \\
White-box $+$ differentiable detector surrogate (ensemble-complete BPDA) & per-image & Table~\ref{tab:adaptive_pgd} & all-input $0.479$; successful-only $0.866$; undetected-success $0.074$ \\
Surrogate-free, exact score (NES on $S(x)$) & per-image, fast-query & Tables~\ref{tab:direct_score},~\ref{tab:direct_score_sacd} & single-shot ${\to}0.16$ ($c{=}8$); full-system miss $0.03$ with \sacd \\
Dual-adaptive, evades \sacd (slow-ramp / ${\approx}2$-query) & per-image reset & Table~\ref{tab:campaign_robust} & miss $0.27$--$0.47$ \\
Dual-adaptive, evades \sacd & session-level & Table~\ref{tab:campaign_robust} & miss $0.002$--$0.026$ \\
Dual-adaptive $+$ identity rotation (Sybil) & per-account & \emph{out of scope} & reverts to per-image reset; needs account rate-limit / cost \\
Benign covariate shift (CIFAR-10-C) & --- & Tables~\ref{tab:cert_shift},~\ref{tab:cert_restore} & FPR $3.9$--$28{\times}\,\alpha$; online recal.\ restores ${\approx}\alpha$ \\
\bottomrule
\end{tabular}
\caption{Threat-model coverage map (all entries are existing measurements,
cross-referenced to their source tables). Each adversary tier is matched to the
experiment that measures it and the residual miss/FPR it leaves. The progression
from per-image to session to per-account granularity makes the load-bearing role
of \sacd explicit and names the Sybil boundary of the C3 robustness claim.}
\label{tab:threat_model}
\end{table}

% ── Main results ──────────────────────────────────────────────────────────
\subsection{Attack Detection Results}
\label{sec:detection}

\begin{table}[t]
\centering
\small
\input{tables/main_attacks.tex}
\caption{%
  Attack detection results generated directly from the Vast.ai result JSONs.
  TPR is adversarial detection rate; FPR is clean false-positive rate.
  The final row is the unweighted mean of the five per-attack TPRs (a
  macro-average across attack families), \emph{not} a pooled-binomial
  meta-estimate over the $25{,}000$ adversarials; with equal per-attack
  $n{=}5000$ the two point estimates coincide ($0.939$), so we report the
  macro-average and claim no pooled confidence interval for it.
  The \emph{AutoAttack (ensemble)} row is the detection rate on the
  standard AutoAttack ensemble's final (per-image worst-case)
  adversarials, which are dominated by high-confidence gradient
  perturbations; it is \emph{not} a claim about every component in
  isolation. Per-sub-attack detection is broken out in
  Appendix~\ref{app:autoattack}, where the weakest standalone component
  (black-box Square) is detected at $0.706$ @ L1 and APGD-DLR at $0.911$
  @ L1.
}
\label{tab:main}
\end{table}

\begin{table}[t]
\centering
\small
\begin{tabular}{lcccc}
\toprule
Attack & TPR & FPR & AUROC & AUPR \\
\midrule
FGSM & 0.897 & 0.075 & 0.968 & 0.961 \\
PGD-40 & 0.991 & 0.075 & 0.996 & 0.996 \\
Square & 0.884 & 0.075 & 0.960 & 0.961 \\
CW-L2 & 0.940 & 0.075 & 0.977 & 0.977 \\
AutoAttack & 1.000 & 0.075 & 1.000 & 1.000 \\
\bottomrule
\end{tabular}
\caption{Threshold-free detection metrics on CIFAR-10/ResNet-18, from a fixed-seed
rerun (5 seeds, $n{=}1000$ clean and $n{=}1000$ adversarial per seed) under the
same disjoint-window protocol. AUROC/AUPR are computed from the raw ensemble
anomaly scores (5-seed mean, per-seed std $\le 0.002$); the paired TPR/FPR at the
L1 operating point reproduce Table~\ref{tab:main} to within $1.5$~pp
(Square, CW-L2, and AutoAttack fall inside the pooled 95\% CIs; FGSM
$0.897$ vs.\ $0.882$ and PGD $0.991$ vs.\ $0.987$ sit just outside,
consistent with rerun-level seed/training variance). AUROC
$\ge 0.96$ on every attack family confirms that clean/adversarial separation holds
across all thresholds, not only at the calibrated operating point.}
\label{tab:auroc}
\end{table}

\paragraph{Threshold-free metrics.}
Table~\ref{tab:auroc} reports AUROC and AUPR for the primary CIFAR-10/ResNet-18
setting. Every attack family exceeds $0.96$ AUROC, with PGD and AutoAttack near
$1.0$, so the detection signal is not an artifact of the calibrated L1 threshold.
These numbers come from a fixed-seed rerun that emits per-sample anomaly scores;
the operating-point TPR/FPR track Table~\ref{tab:main} to within $1.5$~pp
(per-attack agreement detailed in the caption).

\paragraph{Key observations.}
The paper reports only numbers produced by the locked artifact pipeline.
Attack-specific conclusions are drawn from the generated table and the
gate summary emitted by \texttt{run\_vastai\_full.sh} and
\texttt{run\_vastai\_cifar100.sh}; the ViT transfer rows are from
\texttt{run\_vastai\_vit\_cifar10.sh}. No hand-entered historical values
are used.

% ── Multi-backbone / multi-dataset ───────────────────────────────────────
\subsection{Multi-Backbone and Multi-Dataset Validation}
\label{sec:multi_backbone}

To test whether \prism's detection generalizes beyond the main CIFAR-10
ResNet-18 setting, we re-ran the full detection and baseline protocol on
(i) CIFAR-10 with a WRN-28-10 backbone and (ii) CIFAR-100 with a
ResNet-18 backbone. We additionally run a standard-attack-only
transformer transfer check on (iii) CIFAR-10 with ViT-B/16, using the
same calibration scheme ($\alpha \in \{0.10, 0.03, 0.005\}$) and the
same five seeds ($n=1000$ adversarials per attack per seed). The ViT
rows do not include CW-L2, AutoAttack, adaptive PGD, latency, or
baselines. To answer the ``CIFAR-only 32$\times$32 toy benchmark''
objection at scale, we additionally run (iv) a resolution-scaling check
on ImageNet-100 with a fine-tuned ResNet-50 at $224\times224$ (100
classes; standard FGSM/PGD-50/Square only; 3 seeds, $n=1000$
adversarials per attack per seed). The ImageNet-100 rows likewise carry
no CW-L2, AutoAttack, adaptive-PGD, latency, or baseline claim.

\begin{table}[h]
\centering
\small
\input{tables/main_attacks_multi.tex}
\caption{%
  Cross-architecture and cross-dataset detection.
  All four settings clear the L1 design FPR ($\le 0.10$);
  per-tier FPR (L2/L3) values are reported in
  Appendix~\ref{app:cifar100} and the per-seed JSONs.
  WRN-28-10 lifts CIFAR-10 TPR by $+5$--$10$~pp over ResNet-18 across
  FGSM/Square; CIFAR-100 is harder for score-based and
  optimisation-based attacks (Square $0.672$, CW $0.687$) while
  white-box PGD and AutoAttack remain at $\ge 0.98$.
  The ViT-B/16 rows are standard-attack transformer-transfer results
  only: FGSM, PGD-50$\times$10, and Square-5000 at FPR $0.083$.
  Every cell pools 5 fixed seeds $\times n{=}1000 = 5000$ adversarials
  (one canonical run per seed; earlier WRN counts that summed seed-42
  re-runs are de-duplicated here).
  PGD configuration is artifact-accurate per setting:
  CIFAR-10/ResNet-18 uses PGD-40 (1 restart, the ablation-pipeline
  config of \texttt{results\_ablation\_multiseed.json}), whereas
  WRN-28-10, CIFAR-100, and ViT-B/16 use the RobustBench-standard PGD-50
  (10 restarts). Re-running CIFAR-10/ResNet-18 at PGD-50$\times$10 yields
  $0.991$ (Appendix~\ref{app:audit}), statistically tied with the
  PGD-40 $0.987$, so the detection result is invariant to the stronger PGD.
  The ImageNet-100/ResNet-50 rows ($224\times224$, 100 classes, 3 seeds
  $\times n{=}1000 = 3000$, PGD-50$\times$10) hold FGSM $0.924$, PGD
  $1.000$, and Square $0.852$ at pooled FPR $0.065$ (per-tier L2 $0.012$,
  L3 $0.001$); the split-conformal certificate stays valid despite the
  clean anomaly score scale being $\sim$100$\times$ larger than CIFAR,
  confirming the quantile-based guarantee is scale-invariant.
}
\label{tab:main_multi}
\end{table}

\begin{table}[h]
\centering
\small
\input{tables/baselines_multi.tex}
\caption{%
  Design-FPR ($\le 10\%$) baseline comparison across all three settings
  (5 seeds, $n=1000$). The comparison is at exactly matched FPR only on
  CIFAR-10/ResNet-18; on WRN-28-10 and CIFAR-100 three baselines exceed
  the $10\%$ design target (disclosed below) and are reported unfiltered.
  \prism is the only detector that holds $\ge 0.8$ mean TPR
  on all three settings while keeping FPR below the design target;
  every baseline collapses on at least one attack in every setting
  (ODIN/Energy on PGD; LID on Square; Mahalanobis on FGSM/Square on
  CIFAR-100). The recent unsupervised detector SID
  \cite{tian2021detecting} (2021) is the weakest in every setting
  (mean TPR $0.15$--$0.40$), confirming that PRISM's margin is not an
  artifact of comparing only against pre-2021 baselines.
  \textbf{FPR-target disclosure}: in the WRN-28-10 setting,
  Mahalanobis ($0.122$) and LID ($0.105$) exceed the $\alpha{=}0.10$
  design FPR; in the CIFAR-100 setting, ODIN ($0.116$) and SID
  ($0.109$) exceed it.
  Re-calibrating these detectors at a tighter conformal $\alpha{=}0.073$
  (matched to \prism's empirical FPR) would lower their reported TPR
  further; the present table reports the published-protocol values
  unfiltered for transparency.
}
\label{tab:baselines_multi}
\end{table}

\begin{figure}[h]
  \centering
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig_baselines_cifar10.pdf}
    \caption{CIFAR-10 (WRN-28-10).}
    \label{fig:bars_c10}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig_baselines_cifar100.pdf}
    \caption{CIFAR-100 (ResNet-18).}
    \label{fig:bars_c100}
  \end{subfigure}
  \caption{Per-attack TPR at the design FPR target ($\le 0.10$; not all
  baselines hit it on WRN/CIFAR-100, see Table~\ref{tab:baselines_multi}).
  \prism (blue) is
  the only detector that holds $\ge 0.85$ on at least three of four
  attacks per dataset; ODIN/Energy collapse to $0$ on PGD; LID and
  Mahalanobis collapse on Square or FGSM. AutoAttack column has only
  \prism because baselines were not run on AutoAttack.}
  \label{fig:baselines_panel}
\end{figure}

\begin{figure}[h]
  \centering
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig_tiers_cifar10.pdf}
    \caption{CIFAR-10 (WRN-28-10).}
    \label{fig:tiers_c10}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig_tiers_cifar100.pdf}
    \caption{CIFAR-100 (ResNet-18).}
    \label{fig:tiers_c100}
  \end{subfigure}
  \caption{\prism three-tier operating points (L1, L2, L3) per attack.
  AutoAttack and PGD saturate near $\mathrm{TPR}{=}1$ even at the
  tight L3 FPR (${\le}0.005$); Square trades TPR for tighter FPR most
  steeply, consistent with its score-based attack signature being the
  weakest of the four in the ensemble feature space.}
  \label{fig:tiers_panel}
\end{figure}

\paragraph{Cross-setting take-aways.}
(i)~\prism transfers to a deeper architecture (WRN-28-10) without
re-tuning: the WRN run lifts every CIFAR-10 attack TPR while holding
FPR at $0.073$, indicating that the discriminative signal is in the
ensemble channels, not in a ResNet-18-specific calibration.
(ii)~Cross-dataset to CIFAR-100 (10$\times$ more classes), white-box
gradient attacks remain trivially detectable (PGD-50 $0.984$,
AutoAttack $0.999$), but score-based Square ($0.672$) and the
optimisation-based CW-L2 ($0.687$) drop below the $0.85$ design target.
Per-channel ablation (Appendix~\ref{app:cifar100}, Table~\ref{tab:c100_abl})
shows the gap is concentrated in the StabilityV2 channel, suggesting
per-dataset retuning of stability features as a natural next step.
(iii)~The ViT-B/16 transfer run clears the standard-attack gate with
FGSM $1.000$, PGD-50 $1.000$, and Square $0.9998$ TPR at FPR $0.0828$,
supporting backbone-transfer activation monitoring beyond CNN
residual blocks. This row is not evidence for ViT latency, CW-L2,
AutoAttack, or adaptive robustness because those runs were not executed.
(iv)~Even where \prism underperforms its $0.85$ target on CIFAR-100,
it still beats the strongest baseline on every CIFAR-100 attack
(Square: $+22.7$~pp vs ODIN; FGSM: $+6.8$~pp vs ODIN).
Figure~\ref{fig:baselines_panel} visualises both points; the steeper
L3 trade-off on CIFAR-100 in Figure~\ref{fig:tiers_panel} (subfigure
\ref{fig:tiers_c100}) reflects the larger 100-class label space.
(v)~The ImageNet-100/ResNet-50 run scales the detector from
$32\times32$ to $224\times224$ inputs with no architecture or feature
changes and clears the standard-attack gate (FGSM $0.924$, PGD $1.000$,
Square $0.852$ at FPR $0.065$). Critically, the conformal FPR
certificate holds even though the clean Wasserstein anomaly scores are
$\sim$100$\times$ larger in absolute magnitude than on CIFAR (clean mean
$\approx 7.6$ vs.\ $\approx 0.05$): the certificate is a quantile of the
calibration scores, so absolute scale is immaterial to the guarantee.
This directly rebuts the ``CIFAR-only toy benchmark'' objection. Like the
ViT row, it is not evidence for ImageNet-scale latency, CW-L2,
AutoAttack, or adaptive robustness, none of which were executed.

% ── Adaptive attacks ──────────────────────────────────────────────────────
\subsection{Adaptive Attack Evaluation}
\label{sec:adaptive}

Following Carlini et al.~\cite{carlini2019evaluating}, we evaluate
\prism against a white-box adaptive attacker that crafts perturbations
explicitly suppressing the detector's signals. The canonical adaptive
attack used to generate Table~\ref{tab:adaptive_pgd} is the
ensemble-complete BPDA-style PGD launched with
\texttt{run\_adaptive\_pgd.py --through-scorer --ensemble-complete},
whose loss is
\[
  \mathcal{L} = -\mathcal{L}_\text{CE}(x', y_\text{pred})
    \;+\; \lambda \sum_{\ell} \tfrac{1}{D_\ell}\|a_\ell(x') - a_\ell(x)\|_2
    \;+\; \beta_m \|\phi(x')-\phi(x)\|_1
    \;+\; \beta_s q_\theta(x'),
\]
where $a_\ell(x)$ is the per-sample clean activation at monitored layer
$\ell$, $\phi$ is a differentiable surrogate vector for the deployed
ensemble side channels (DCT high-frequency energy, softmax entropy,
logit profile, StabilityV2, and input gradient norm), and
$q_\theta$ is the fitted side-quadratic logistic surrogate used to
attack cross-channel interactions in the ensemble scorer. Thus
$\lambda{=}0$ is not non-adaptive PGD: the ensemble side-channel
surrogate remains active, while $\lambda$ controls the additional
activation-matching pressure on \tamm. We first sweep
$\lambda \in \{0, 0.5, 1, 2, 5, 10\}$ at $100$ PGD steps with $10$ random
restarts on a small $5{\times}50$ Monte Carlo scan, then confirm the
worst setting ($\lambda{=}10$) on $5{\times}200$ samples. Runs use the
deterministic \prism forward pass (deterministic scoring and
hash-based TDA subsampling), so EOT gradient averaging is a mathematical
no-op: the per-seed metadata records the intended verification setting
(\texttt{eot\_samples}=1, \texttt{eot\_verify\_samples}=20) but the attack
is \emph{not} re-run with $20$ resampling passes, because repeated draws
would produce identical gradients. A confirmatory
\texttt{--eot-samples 20} re-run is left as a (no-op) verification step.

\begin{table}[h]
\centering
\small
\input{tables/adaptive_pgd.tex}
\caption{%
  Ensemble-complete adaptive PGD (100 steps, 10 restarts, 5 seeds).
  The scan rows use $n{=}50$/seed to select the worst $\lambda$; the
  confirm row reruns $\lambda{=}10$ at $n{=}200$/seed. Overall TPR falls
  to $0.479$ on the confirmation, but among adversarials that actually
  fool the backbone the detector still catches $0.866$; the undetected
  successful-attack rate is $0.074$. The independent clean calibration
  gate passes at L1/L2/L3 FPR $0.079/0.026/0.003$.
}
\label{tab:adaptive_pgd}
\end{table}

\paragraph{Honest framing.} The ensemble-complete surrogate is a much
stronger attacker than the earlier two-channel \texttt{--through-scorer}
loss: it jointly attacks TDA activation matching and differentiable
surrogates for the scorer's non-TDA side channels. It substantially
reduces all-input CIFAR-10 detection, with confirmed TPR
$0.479$ at $\lambda{=}10$. However, because many high-$\lambda$
perturbations no longer fool the classifier, attack success rate also
falls to $0.552$; conditioning on successful adversarials, \prism still
detects $0.866$ and leaves $0.074$ of all attempted inputs as both
successful and undetected. FPR is governed by the fixed conformal
threshold: the held-out calibration gate passes at L1/L2/L3 FPR
$0.079/0.026/0.003$, while one $n{=}200$ confirmation seed has a noisy
L3 estimate of $2/200=0.010$ and the pooled confirmation is exactly
$0.005$. We therefore frame the confirmation as a five-seed Monte Carlo
stress test, not a disjoint-$1000$ clean benchmark.

% ── Direct surrogate-free score attack ────────────────────────────────────
\subsection{Direct Surrogate-Free Score Attack and Campaign Rescue}
\label{sec:direct_score}

The adaptive attack of \S\ref{sec:adaptive} attacks differentiable
\emph{surrogates} for the non-TDA side channels, leaving open the objection
that the surrogate gap---not robustness---explains residual detection. We
close this with a \emph{surrogate-free} attack that estimates the gradient of
the \emph{exact deployed} score $S(x)$ by NES finite differences (antithetic
sampling, $Q{=}12$ pairs, $\sigma{=}0.01$) and ascends the combined objective
$J(x) = \mathcal{L}_\text{CE}(f(x),y) - c\,S(x)$, with both gradients
\emph{unit-normalised} before combining so the trade-off weight $c$ acts as a
true dial rather than an on/off switch. The classifier is white-box; the
detector is reachable only through its scalar score. All runs use
$\varepsilon{=}8/255$ and $n{=}100$ evaluation images sampled (seed $42$)
from the held-out evaluation window (test indices $[8000,10000)$).

\begin{table}[h]
\centering
\small
\begin{tabular}{lccc}
\toprule
$c$ (evasion weight) & model ASR & per-sample TPR & undetected-success \\
\midrule
0.0 & 1.00 & 0.990 & 0.010 \\
0.5 & 1.00 & 0.960 & 0.040 \\
1.0 & 1.00 & 0.940 & 0.060 \\
2.0 & 1.00 & 0.870 & 0.130 \\
4.0 & 0.99 & 0.620 & 0.380 \\
8.0 & 0.85 & 0.160 & 0.740 \\
\bottomrule
\end{tabular}
\caption{%
  Surrogate-free direct-score attack on CIFAR-10/ResNet-18, \emph{per-sample}
  detector only (campaign monitor off). NES runs on the exact deployed $S(x)$,
  so the BPDA surrogate gap is removed. At $c{=}2$ the attack reproduces the
  \S\ref{sec:adaptive} adaptive-PGD operating point
  ($0.87$ per-sample TPR) with no surrogate, confirming the surrogate gap is
  not hiding robustness. As evasion pressure rises the single-shot detector
  degrades ($\mathrm{TPR}{\to}0.16$ at $c{=}8$): the expected
  Carlini--Wagner outcome for any single-shot detector, so $0.866$ is a point
  on a curve, not a floor.
}
\label{tab:direct_score}
\end{table}

\paragraph{The campaign monitor is load-bearing.} The surrogate-free attack
needs ${\approx}960$ score queries per image to converge, so the attack is
\emph{itself a campaign}. With the deployed \sacd CUSUM monitor live
(\texttt{models/l0\_thresholds.pkl}; every NES probe flows through it; the
monitor is \emph{reset per image}, the hardest case for the defender), the
full system catches what the single-shot tier alone misses
(Table~\ref{tab:direct_score_sacd}).

\begin{table}[h]
\centering
\small
\begin{tabular}{lcc}
\toprule
 & $c{=}4$ & $c{=}8$ \\
\midrule
per-sample TPR                                  & 0.68 & 0.26 \\
campaign caught (\sacd)                          & 0.95 & 0.91 \\
median queries-to-alert                          & 40   & 54 \\
\textbf{system TPR} (tier $\lor$ \sacd)          & \textbf{0.96} & \textbf{0.92} \\
\textbf{undetected full-system miss}             & \textbf{0.03} & \textbf{0.03} \\
\bottomrule
\end{tabular}
\caption{%
  Same surrogate-free attack, \emph{full} \prism (per-sample tier OR \sacd
  campaign monitor). \sacd fires at $40$--$54$ of the ${\approx}960$ required
  queries ($<6\%$ of the budget): the attacker drives $S(x)$ \emph{down}, but
  the early transient (random $\pm\varepsilon$ initialisation and
  pre-convergence probes) trips the upward-drift CUSUM. For this
  \emph{fast-query} attacker the full-system miss is $0.03$, flat across
  $c{=}4$ and $c{=}8$; \S\ref{sec:campaign_robust} shows this is \emph{not} a
  universal floor under attackers that evade \sacd itself. Per-sample TPR is \emph{higher} here than in
  Table~\ref{tab:direct_score} ($0.16{\to}0.26$, $0.62{\to}0.68$) because a
  fired campaign monitor tightens the per-sample tier
  (\texttt{classify}$(\cdot, l0\_\text{active}{=}\text{True})$ applies the
  $\lambda{=}0.8$ factor of \S\ref{sec:sacd}); the two layers reinforce.
  The monitor is reset per image here; \S\ref{sec:campaign_robust} shows a
  slow-ramp or minimal adaptive attacker exploits exactly this per-image reset,
  and that session-level monitoring restores the low miss.
}
\label{tab:direct_score_sacd}
\end{table}

\paragraph{Scope, and the dual-adaptive attacker.} This attack adapts against
$S(x)$, not against \sacd. We now run the \emph{dual-adaptive} attacker the
earlier draft left as future work (\S\ref{sec:campaign_robust}): a slow-ramp
that holds $S(x)$ under the CUSUM knee, and a minimal-perturbation
${\approx}2$-query attacker that never probes the detector. Under the deployed
\emph{per-image reset} these raise the full-system miss to $0.27$ and $0.47$,
so $0.03$ is \emph{not} a universal floor; removing the per-image reset
(session-level monitoring) restores it to $0.002$ and $0.026$. Campaign
detection is thus load-bearing for adaptive robustness (contribution C3), but
only at \emph{session} rather than per-image granularity.

% ── Campaign-monitor adaptive robustness ──────────────────────────────────
\subsection{Campaign-Monitor Adaptive Robustness (Dual-Adaptive Attack)}
\label{sec:campaign_robust}

The $0.03$ rescue of \S\ref{sec:direct_score} is measured against a fast-query
attacker whose probing \emph{is} a campaign. We stress it with two attackers
designed to evade \sacd itself, on the deployed CIFAR-10/ResNet-18 scorer and
the calibrated \texttt{l0\_thresholds.pkl} (CUSUM knee $S{=}1.92$; every query
streamed through the live monitor; 5 seeds, $n{=}100$/seed): (i) a
\emph{score-budgeted slow-ramp} that ascends \(\mathcal{L}_\text{CE}\) while
holding $S(x)$ below the CUSUM knee---which also places the adversarial below
the per-sample L1 threshold $2.81$; and (ii) a \emph{minimal-perturbation}
${\approx}2$-query attacker that never probes the detector. Each is evaluated
under the deployed \emph{per-image reset} and under \emph{session-level}
monitoring (the change detector persists across the attacker's stream).

\begin{table}[h]
\centering
\small
\input{tables/campaign_robustness.tex}
\caption{%
  Dual-adaptive campaign attack vs.\ \sacd (CIFAR-10/ResNet-18, 5 seeds,
  $n{=}500$/cell). Under the deployed \emph{per-image reset}, the slow-ramp and
  minimal attackers raise the full-system miss to $0.272$ and $0.472$: the
  $0.03$ of Table~\ref{tab:direct_score_sacd} is \emph{not} a universal floor.
  \emph{Session-level} monitoring restores it to $0.002$ and $0.026$; the
  minimal residual shrinks with session length ($0.11$ at $n{=}20$ to $0.026$
  at $n{=}500$), i.e.\ the change detector asymptotically re-accumulates the
  signal. The anchor row (faithful high-query attacker) reproduces the $0.03$
  of Table~\ref{tab:direct_score_sacd}, validating the harness.
}
\label{tab:campaign_robust}
\end{table}

\paragraph{Take-away.} Per-image reset---advertised in \S\ref{sec:direct_score}
as the defender's hardest case---is in fact the \emph{exploitable} case: it
grants every input a fresh warm-up window an adaptive attacker can flip within
before the CUSUM accumulates. Session- or account-scoped monitoring, in which
the change detector persists across an attacker's queries, restores the low
miss and is the deployable configuration. We therefore scope contribution C3 to
session-level monitoring, and report the per-image-reset numbers as the honest
adaptive-stress result rather than a robustness guarantee. Session-scoped
monitoring assumes the adversary cannot cheaply acquire fresh sessions: an
attacker who rotates identities (one query stream per account) reverts the
monitor to the exploitable per-image-reset regime, so the deployable
configuration pairs session-level detection with account-level rate-limiting
or cost (the orthogonal Sybil control). We scope the C3 robustness claim to
this single-session threat model.

% ── Ablation ──────────────────────────────────────────────────────────────
\subsection{Ablation Study}
\label{sec:ablation}

We ablate seven configurations:
(1)~Full \prism;
(2--5)~four channel-mask arms that zero one ensemble feature family at
scoring time --- No-LogitProfile, No-StabilityV2, No-GradNorm, No-DCT ---
each isolating a distinct channel's contribution;
(6)~Ensemble-no-TDA, which removes persistence features while keeping
softmax-entropy, gradient-norm, DCT, and stability channels; and
(7)~TDA-only with its own base-Wasserstein conformal calibrator.
The channel-mask design replaces an earlier "No MoE" arm that was
statistically indistinguishable from Full \prism ($t{=}0$, $p{=}1$ across all
attacks) because the MoE router is a downstream gating module that does not
alter ensemble scores; the new arms instead surface each feature family's
attack-specific failure mode (Table~\ref{tab:ablation}). SACD/L0 is
evaluated separately in campaign streams because it is a temporal mechanism,
not an IID detector.

\begin{table}[h]
\centering
\small
\input{tables/ablation.tex}
\caption{%
  IID ablation generated from the multi-seed ablation JSON
  (5 seeds, $n=1000$ per seed). C1 is evaluated as the marginal gap between
  Full \prism and Ensemble-no-TDA (mean TPR $0.918 - 0.787 = +13.1$~pp).
  Each channel-mask arm exposes a distinct failure mode:
  No-LogitProfile FPR explodes to 0.938 (the logit channel is the
  FPR-controller); No-StabilityV2 PGD collapses to 0.396 (stability is the
  PGD signal); No-GradNorm FPR triples to 0.218 (gradient norm is a
  counter-evidence channel); No-DCT FGSM drops to 0.751 (DCT is the FGSM
  signal). All channel arms reach $p<0.001$ on at least one attack.
  \textbf{Reading guide (matching Table~\ref{tab:c100_abl}).} Rows whose
  mean FPR exceeds the $0.10$ design target --- No-LogitProfile ($0.938$),
  No-GradNorm ($0.218$), and No-StabilityV2 ($0.107$) --- are threshold
  collapses, not improved detection: their higher mean TPR is bought by
  flagging far more clean inputs and is non-deployable. Only Full \prism
  ($0.074$), No-DCT ($0.098$), and Ensemble-no-TDA ($0.077$) sit at the
  design FPR, so only their TPRs are comparable to Full \prism.
}
\label{tab:ablation}
\end{table}

\paragraph{Baseline comparison.}
Table~\ref{tab:baselines} reports four baseline detectors at the same
$\alpha{=}0.10$ design target, aggregated across the same 5 seeds. \prism
Full has both the lowest empirical FPR ($0.074$) and the highest mean TPR
($0.918$), outperforming the strongest baseline (Mahalanobis, mean TPR
$0.582$ at FPR $0.077$) by $+33.6$~pp mean TPR. PRISM is the only
detector that does not collapse on at least one attack family
(ODIN/Energy: PGD TPR $\approx 0.001$; LID: Square TPR $0.170$).
Energy's empirical FPR ($0.105$) modestly exceeds its $\alpha{=}0.10$
design target on this test split, which we report unfiltered; recomputing
at a tighter conformal $\alpha{=}0.075$ (matched to \prism's empirical
FPR) would lower Energy's mean TPR further.
\emph{Supervision disclosure.} \prism is a supervised detector (its ensemble
head is trained on an FGSM/PGD/Square mix, \S\ref{sec:tamm}), whereas the four
detectors here are run as unsupervised variants (clean-only threshold
calibration); part of \prism's margin reflects this supervision. For a
like-for-like comparison against \emph{supervised} detectors we benchmark
SpectralDefense in Appendix~\ref{app:recent_baselines}, where \prism still wins
--- decisively on CW/AutoAttack, which it never trains on.
We additionally benchmark two recent (2021) detectors---SID
\cite{tian2021detecting} and the supervised SpectralDefense
\cite{harder2021spectraldefense}---in Appendix~\ref{app:recent_baselines}.
Both underperform \prism: SID across all attack families, and
SpectralDefense everywhere except the gradient-$L_\infty$ attacks it is
trained on, collapsing on black-box (Square) and $L_2$ (CW) attacks even
in-distribution (Table~\ref{tab:recent_baselines}) and failing to transfer
across attack families (Table~\ref{tab:spectral_cross}).

\begin{table}[h]
\centering
\small
\input{tables/baselines.tex}
\caption{%
  Matched-FPR baseline comparison (5 seeds, $n=1000$ per seed).
  \prism Full beats the best baseline by $+33.6$~pp mean TPR while holding
  the lowest FPR.
}
\label{tab:baselines}
\end{table}

\paragraph{Temporal and recovery contributions.}
SACD/L0 is reported from campaign-stream experiments (\S\ref{sec:campaign})
and TAMSH from L3 recovery experiments (\S\ref{sec:recovery}). Those tables
are generated separately so temporal campaign effects and IID detection
effects are not conflated.

% ── Score distribution ────────────────────────────────────────────────────
\subsection{Score Distribution Analysis}
\label{sec:scored}

\paragraph{Reporting.}
Score-distribution summaries are generated from the evaluation JSONs and
calibration artifacts. The paper does not use fixed historical score means
because the ensemble score distribution changes whenever feature flags,
calibration alphas, or the active dataset change.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.75\linewidth]{figures/fig2_score_dist.pdf}
  \caption{Score distributions for clean and adversarial inputs
           (CIFAR-10, ResNet-18, single-attack reference).
           Dashed lines: conformal thresholds L1, L2, L3.
           Clean and PGD-40 distributions are clearly separated;
           FGSM at low $\varepsilon$ overlaps the clean tail.}
  \label{fig:score_dist}
\end{figure}

\begin{figure}[h]
  \centering
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig_score_dist_cifar10.pdf}
    \caption{CIFAR-10 (WRN-28-10).}
    \label{fig:sd_c10}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig_score_dist_cifar100.pdf}
    \caption{CIFAR-100 (ResNet-18).}
    \label{fig:sd_c100}
  \end{subfigure}
  \caption{Per-attack score envelopes (p5--p95 whiskers, IQR boxes,
  medians) of the fused ensemble score $S(x)$ for clean (blue) vs
  adversarial (red) inputs across all four attacks per dataset. $S(x)$
  is signed: clean inputs centre near zero (CIFAR-10 clean median
  $\approx 0.3$) and attacks push it positive. AutoAttack pushes scores
  furthest from the clean median (CIFAR-10 adversarial median
  $\approx 17$); Square has the tightest separation, matching the lower
  Square TPR in Table~\ref{tab:main_multi}. CIFAR-100 (right) shows the
  same ordering with a slightly larger clean spread (clean median
  $\approx 0.04$, p5--p95 width $5.9$ vs $4.1$ on CIFAR-10), reflecting
  the 100-class softmax temperature. This is the same $S(x)$ scale as
  the per-attack histograms in Appendix~\ref{app:score_hist}
  (clean mean $0.27$).}
  \label{fig:score_dist_panel}
\end{figure}

% ── Campaign detection ───────────────────────────────────────────────────
\subsection{Campaign Detection (SACD): Latency, Not ASR Gap}
\label{sec:campaign}

We evaluate L0 campaign detection on clean-only, sustained ($\rho \in
\{0.5, 0.8, 1.0\}$), low-rate ($\rho{=}0.05$), and burst stream scenarios
generated by \texttt{run\_campaign\_eval.py}.

\paragraph{Headline metric: time-to-detect.}
On sustained $\rho{=}1.0$ streams, the L0 monitor flags the campaign within
a mean of $\mathbf{2.6 \pm 0.5}$ queries after the first adversarial step
(across 5 seeds; Table~\ref{tab:campaign}). On $\rho{=}0.5$ streams,
detection takes $3.4 \pm 0.9$ queries; on 10-step burst streams,
$12.0 \pm 0.0$ queries; on low-rate $\rho{=}0.05$ streams,
$17.4 \pm 8.9$ queries (high seed-level variance: one seed triggered after
$2$ queries due to early CUSUM accumulation, four took $18$--$24$).

\begin{table}[h]
\centering
\small
\input{tables/campaign.tex}
\caption{%
  Campaign detection across stream scenarios (5 seeds). The
  headline metric is mean time-to-detect; clean-only L0 activation fraction
  is 0.000 on all 5 seeds with the CUSUM drift floor $\delta{=}1.0$.
}
\label{tab:campaign}
\end{table}

\paragraph{Why time-to-detect, not ASR gap.}
A naive headline metric ---
adversarial success rate (ASR) reduction with L0 on vs.\ off at
$\rho{=}1.0$ --- is structurally bounded by the upstream L3 conformal
threshold, which already rejects approximately 99\% of $\rho{=}1.0$
adversarials within 22 queries, leaving no room for L0 to contribute an
additional ASR-reduction gap. We therefore report ASR gap in the appendix
for completeness, but treat detection latency (time-to-first-trigger after
campaign onset) as the operationally meaningful metric: a defender that
learns of an attack within 3 queries can deploy mitigations
(rate-limiting, account quarantine, model rollback) before the attacker
has issued more than a handful of queries against the deployed system.

\paragraph{Clean-only false alarm rate.}
With the CUSUM drift floor $\delta{=}1.0$ (\S\ref{sec:sacd}), L0 activation
fraction on the 500-step calibration clean streams is $0.000$ on all 5 seeds
(with $\delta{=}0$ it reaches up to $0.594$ on 2 of 5 seeds). For 50-query
clean windows (warmup$=20$, 30
at-risk steps), empirical false alarm probability is $< 5\%$.
A stress test on longer clean streams (bootstrap-resampled from
$n{=}1{,}000$ real post-fusion ensemble scores generated end-to-end on
the VAL split) confirms that the deployed $C^*=5.0$ holds the
$0.000$ active-fraction through $1{,}000$ steps and starts admitting
its first spurious trigger between step $\approx 1{,}800$ and step
$\approx 4{,}500$ on real long streams
(Appendix~\ref{app:l0_stress}, Table~\ref{tab:l0_stress}). The
alternative feasible cells $C^* \in \{8.0, 10.0, 12.0\}$ from the
calibration grid hold $0.000$ across all stream lengths up to
$10{,}000$ steps. Longer-window deployments should either pick one of
these conservative cells or periodically reset L0 / auto-deactivate on
return to the clean score baseline.

\paragraph{Two distinct false-alarm rates.}
The \emph{FPR (clean steps)} column in Table~\ref{tab:campaign}
($0.084$--$0.143$) is the per-query L1/L2/L3 conformal alert rate of the
underlying IID detector --- the same FPR reported in
Table~\ref{tab:main} --- not the L0 campaign-trigger rate. A deployment
that quarantines accounts should gate on L0 firing (clean-only rate
$0.000$ with the drift floor), not on every L1 step; L1 alerts are
appropriate for softer signals such as elevated logging or rate-limiting,
where a $\sim$10\% per-query rate is acceptable because the action is
reversible and per-query.

% ── Recovery (TAMSH) ─────────────────────────────────────────────────────
\subsection{L3 Recovery (TAMSH)}
\label{sec:recovery}

We measure recovery accuracy of five routing strategies applied to the
subset of PGD inputs that trigger the L3 (reject) tier: the most aggressive
adversarials, where simply rejecting the input is the conservative default.
We pre-registered a $+15$~pp recovery gap (best router vs.\
passthrough) as the contribution target for this component. The lightweight
topology gate (combined $H_0{+}H_1$ Wasserstein) falls $4.4$~pp short of
that target at $+10.6$~pp (Table~\ref{tab:recovery}); a \emph{learned}
router trained on the L3 pool (logistic regression on final-layer
activations) clears the target, recovering $0.287$ --- $9.1\times$ the
uniform $1/K$ control and $2.1\times$ the topology gate, matching the
prior-augmented upper bound without hardcoding the attack
(Appendix~\ref{app:tamsh_diag}).

\begin{table}[h]
\centering
\small
\input{tables/recovery.tex}
\caption{%
  L3-recovery on PGD adversarials (5 seeds, $n_{L3} \approx 792$ per
  seed; mean trigger rate $0.792$ within sanity band $[0.10, 0.82]$). The
  \texttt{tamsh} row is the lightweight topology gate (combined
  $H_0{+}H_1$), which delivers $+10.6$~pp over passthrough, falling
  $4.4$~pp short of the pre-registered $+15$~pp target; a learned router
  trained on the L3 pool clears the target (Appendix~\ref{app:tamsh_diag}).
  The \texttt{tamsh\_force} row is a prior-augmented upper-bound analysis
  (always-route to PGD specialist, not a learned policy); the oracle
  ceiling is per-input best-of-$K$ on the same L3 subset.
}
\label{tab:recovery}
\end{table}

\paragraph{Router contribution isolation.}
The \texttt{tamsh\_uniform} arm replaces topology-distance gating with
uniform $1/K$ mixing across all 4 experts. Its recovery accuracy equals
that of passthrough ($0.033$ each, paired gap $0.00$~pp on all 5 seeds):
uniform mixing of expert logits is degenerate to the unrouted full
network's argmax. The $+10.6$~pp \texttt{tamsh} gap (combined $H_0{+}H_1$
Wasserstein gating) over uniform is therefore attributable entirely to the
routing signal, not to standalone expert-network capacity --- a positive
but partial result against the pre-registered $+15$~pp target. The further
$+15.4$~pp lift achieved by \texttt{tamsh\_force} (always-route to the
PGD-trained expert) reflects correct routing under a strong prior: L3-
rejected inputs are by definition high-confidence adversarials, and PGD
is the dominant adversarial signal in our training mix. We treat
\texttt{tamsh\_force} as an upper-bound analysis rather than a deployable
policy, since it bakes in an oracle assumption about attack identity that
a real router would have to learn online. The gap between the topology
gate ($0.139$ recovery accuracy) and the prior-augmented variant
($0.293$) bounds the headroom for a stronger gating signal below the
per-input best-of-$K$ oracle ceiling ($0.324 \pm 0.007$, recomputed on the
same-protocol 5-seed PGD L3 pool, $n_{L3}{\approx}796$/seed,
App.~\ref{app:tamsh_diag}); a \emph{learned} router trained on the L3 pool
closes most of it, reaching $0.287$ (matching the prior-augmented variant
without its hardcoded prior; Appendix~\ref{app:tamsh_diag}).

% ── Calibration ──────────────────────────────────────────────────────────
\subsection{Calibration Sensitivity}
\label{sec:calibration}

Figure~\ref{fig:calib} sweeps $n_\text{cal} \in \{500, 1000, 2000, 3000\}$
and confirms threshold stability: the L3 threshold varies by $\pm 0.03$
(range $0.06$) across calibration set sizes (25 bootstrap subsamples per
size), and the achieved FPR on the held-out validation split tracks the
target $\alpha$ within the Wilson-95\% interval, satisfying
Proposition~\ref{prop:coverage}.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.75\linewidth]{figures/fig3_calibration.pdf}
  \caption{Conformal threshold stability vs.\ calibration set size
           $n_\text{cal}$. \emph{Left}: L1/L2/L3 threshold values (mean
           $\pm$ 95\% bootstrap envelope; L3 half-range $\pm 0.03$ across
           the full sweep). \emph{Right}: Achieved FPR on the held-out
           validation set tracks the target $\alpha$ for all three tiers.
           Source: \texttt{clean\_scores.npy} (2{,}000 calibration scores)
           with 25 bootstrap subsamples per size.}
  \label{fig:calib}
\end{figure}

% ── Certificate under benign covariate shift ──────────────────────────────
\subsection{Certificate Validity Under Benign Covariate Shift (CIFAR-10-C)}
\label{sec:cert_shift}

The \cadg guarantee (Proposition~\ref{prop:coverage}) is exact only under
exchangeability: the clean test distribution must match the calibration
distribution. We audit what happens when this assumption is \emph{benignly}
violated, using CIFAR-10-C \cite{hendrycks2019benchmarking}: 8 corruption
families (gaussian noise, shot noise, defocus blur, motion blur, fog, frost,
contrast, JPEG compression) at severities $\{1,3,5\}$, $n{=}1000$ images per
cell (24 cells, forward passes only, seed 42). Every non-PASS verdict on a
corrupted-but-benign input counts as a false positive against the certified
$\alpha$.

\begin{table}[h]
\centering
\small
\begin{tabular}{lcccccc}
\toprule
 & & \multicolumn{3}{c}{realised FPR by severity} & & cells within \\
\cmidrule(lr){3-5}
Tier & certified $\alpha$ & sev.\ 1 & sev.\ 3 & sev.\ 5 & max & target (of 24) \\
\midrule
L1 & 0.10  & 0.194 & 0.394 & 0.573 & 0.999 & 5 \\
L2 & 0.03  & 0.060 & 0.272 & 0.429 & 0.992 & 6 \\
L3 & 0.005 & 0.009 & 0.139 & 0.278 & 0.920 & 6 \\
\bottomrule
\end{tabular}
\caption{%
  Conformal-certificate audit under benign covariate shift (CIFAR-10-C,
  8 corruptions $\times$ severities $\{1,3,5\}$, $n{=}1000$/cell). The
  certificate degrades severely: the overall mean realised FPR is
  $0.387$ at L1 ($3.9\times$ the certified $0.10$), $0.254$ at L2
  ($8.5\times$), and $0.142$ at L3 ($28\times$), with noise corruptions
  worst (gaussian-noise L1 mean $0.863$) and weather/compression mildest
  (fog $0.120$). At severity 1 the L3 tier nearly holds ($0.009$ vs.\
  certified $0.005$). This is the expected behaviour of split conformal
  prediction when exchangeability is broken
  \cite{tibshirani2019conformal}, not a \prism-specific defect, and the
  failure direction is conservative for safety (over-flagging benign
  inputs costs availability; it never silently admits them). Deployments
  expecting sustained distribution shift should recalibrate online or use
  shift-aware conformal methods \cite{tibshirani2019conformal}.
}
\label{tab:cert_shift}
\end{table}

\paragraph{Reading.} The certificate should be advertised as
\emph{in-distribution} FPR control. Under benign corruption it fails in the
flagging direction --- corrupted inputs look anomalous to the same channels
that flag adversarials --- so the system remains safe but loses availability;
the measured degradation in Table~\ref{tab:cert_shift} replaces the
qualitative caveat of earlier drafts and bounds the operational cost of
deploying \prism under shift without recalibration.

\paragraph{Restoring the certificate online.} The degradation is
\emph{recoverable}. Because broken exchangeability is the cause, re-fitting the
split-conformal quantile on a small held-out slice of the \emph{same}
corrupted-but-benign stream ($n_\text{recal}{=}300$ confirmed-clean images per
corruption$\times$severity cell, evaluated on the disjoint $n_\text{eval}{\approx}700$
remainder) restores realised FPR to the certified target.
Table~\ref{tab:cert_restore} pools the frozen-vs-recalibrated FPR over the same
24 cells: mean L1 FPR falls from $0.386$ to $0.092$ ($\le\alpha{=}0.10$), L2
from $0.253$ to $0.025$, and L3 from $0.142$ to $0.0034$; even the worst noise
cell (gaussian-noise sev.\ 5) recovers from $0.999$ to $0.096$. This is the
certificate analogue of the session-level campaign fix
(\S\ref{sec:campaign_robust}): a measured negative (the certificate breaks
under shift) becomes a measured positive (online shift-aware recalibration
restores it). The recalibration only assumes a small \emph{benign}
(non-adversarial) calibration slice of the shifted stream---the standard
online-conformal-under-covariate-shift assumption
\cite{tibshirani2019conformal}---and does not require knowing the corruption
type.

\begin{table}[h]
\centering
\small
\begin{tabular}{lcccc}
\toprule
Tier & certified $\alpha$ & mean frozen FPR & mean recalibrated FPR & cells $\le\alpha$ (of 24) \\
\midrule
L1 & 0.10  & 0.386 & \textbf{0.092}   & 20 \\
L2 & 0.03  & 0.253 & \textbf{0.025}   & 19 \\
L3 & 0.005 & 0.142 & \textbf{0.0034}  & 16 \\
\bottomrule
\end{tabular}
\caption{%
  Certificate \emph{restoration} under benign covariate shift (CIFAR-10-C, same
  8 corruptions $\times$ severities $\{1,3,5\}$, 24 cells, $n_\text{recal}{=}300$,
  $n_\text{eval}{\approx}700$/cell). \emph{Frozen} reproduces the audit of
  Table~\ref{tab:cert_shift} ($0.386/0.253/0.142$); shift-aware split-conformal
  recalibration on a held-out benign slice of the same corruption restores
  realised FPR to ${\approx}\alpha$ at every tier. Worst cell (gaussian-noise
  sev.\ 5): L1 $0.999{\to}0.096$. The few residual over-target cells lie within
  the Wilson interval at $n_\text{eval}{\approx}700$.
}
\label{tab:cert_restore}
\end{table}
