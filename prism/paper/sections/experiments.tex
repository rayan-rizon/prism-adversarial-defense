\section{Experiments}
\label{sec:experiments}

We evaluate \prism on CIFAR-10 against white-box, black-box,
optimization-based, and adaptive attacks, followed by per-channel ablation,
matched-FPR baseline comparison, campaign-detection latency, and L3
recovery. To assess cross-architecture and cross-dataset generalization,
we additionally report the same detection and baseline protocol on
CIFAR-10/WRN-28-10 and CIFAR-100/ResNet-18
(\S\ref{sec:multi_backbone}, Table~\ref{tab:main_multi}); a full per-table
CIFAR-100 set (adaptive PGD, ablation, recovery, campaign, latency) is
deferred to Appendix~\ref{app:cifar100}.

% ── Setup ──────────────────────────────────────────────────────────────────
\subsection{Experimental Setup}
\label{sec:setup}

\paragraph{Base classifier.}
The main CIFAR-10 results use a CIFAR-native ResNet-18 \cite{he2016deep}
trained from scratch. For cross-architecture validation we additionally
train a WRN-28-10 \cite{zagoruyko2016wide} on CIFAR-10; for cross-dataset
validation we use ResNet-18 on CIFAR-100. \prism monitors activations at $L=3$ residual block outputs weighted
0.30, 0.30, 0.40 (shallow-to-deep) in all settings. For the ResNet-18
backbones (CIFAR-10 and CIFAR-100) these are \texttt{layer2},
\texttt{layer3}, \texttt{layer4}; for the WRN-28-10 backbone (which has
three residual stages, not four) the analogous depth slice is
\texttt{layer1}, \texttt{layer2}, \texttt{layer3} with the same
0.30/0.30/0.40 weights, preserving the shallow-to-deep emphasis.
All inputs remain native $3\times 32\times 32$ images with dataset-specific
CIFAR normalization; no ImageNet pretraining or 224x224 upsampling is used.

\paragraph{PRISM calibration.}
We calibrate thresholds using $n_\text{cal}=2{,}000$ held-out clean images
(split conformal: $n_\text{cal}$ for threshold fitting,
$n_\text{val}=1{,}000$ for coverage verification),
with $\alpha \in \{0.10, 0.03, 0.005\}$.
Thresholds and validation FPRs are generated by the artifact pipeline and
reported from the resulting JSON files rather than manually transcribed.

\paragraph{Attacks.}
We evaluate four attack classes:
\begin{itemize}[leftmargin=*,noitemsep]
  \item \textbf{FGSM} \cite{goodfellow2015explaining}: single-step gradient attack, $\varepsilon=8/255$ ($\ell_\infty$).
  \item \textbf{PGD-40} \cite{madry2017towards}: 40-step projected gradient descent, $\varepsilon=8/255$, step size $\alpha=\varepsilon/4$ ($\ell_\infty$).
  \item \textbf{CW-L2} \cite{carlini2017adversarial}: optimization-based attack, canonical configuration \texttt{max\_iter}=100, \texttt{binary\_search\_steps}=9 (matches Table~\ref{tab:main} \texttt{results\_cw\_n1000\_ms5\_seed*}). A weakened-CW run (\texttt{max\_iter}=5, \texttt{binary\_search\_steps}=5) is reported in Appendix~\ref{app:weak_cw} for detector-fidelity analysis only.
  \item \textbf{Square Attack} \cite{andriushchenko2020square}: black-box score-based attack, 5000 queries, $\varepsilon=8/255$ ($\ell_\infty$).
  \item \textbf{AutoAttack} \cite{croce2020reliable}: standard ensemble evaluation.
  \item \textbf{Adaptive PGD}: BPDA-style activation-matching attack with $\lambda$ sweep, 100 steps, 10 restarts, and EOT verification metadata.
\end{itemize}
All attacks are implemented via Adversarial Robustness Toolbox (ART) v1.20 
\cite{nicolae2018adversarial} or native PyTorch implementations in pixel
$[0,1]$ space; \prism receives CIFAR-normalized inputs after adversarial generation.

\paragraph{Baselines.}
We reproduce four matched-FPR baselines on the same splits:
\textbf{LID} \cite{ma2018characterizing}, \textbf{Mahalanobis}
\cite{lee2018simple}, \textbf{ODIN} \cite{liang2018enhancing}, and
\textbf{Energy} \cite{liu2020energy}.

% ── Main results ──────────────────────────────────────────────────────────
\subsection{Attack Detection Results}
\label{sec:detection}

\begin{table}[t]
\centering
\small
\input{tables/main_attacks.tex}
\caption{%
  Attack detection results generated directly from the Vast.ai result JSONs.
  TPR is adversarial detection rate; FPR is clean false-positive rate.
  The final row is the unweighted mean of the five per-attack TPRs (a
  macro-average across attack families), \emph{not} a pooled-binomial
  meta-estimate over the $25{,}000$ adversarials; with equal per-attack
  $n{=}5000$ the two point estimates coincide ($0.939$), so we report the
  macro-average and claim no pooled confidence interval for it.
}
\label{tab:main}
\end{table}

\paragraph{Key observations.}
The paper reports only numbers produced by the locked artifact pipeline.
Attack-specific conclusions are drawn from the generated table and the
gate summary emitted by \texttt{run\_vastai\_full.sh} and
\texttt{run\_vastai\_cifar100.sh}; no hand-entered historical values are used.

% ── Multi-backbone / multi-dataset ───────────────────────────────────────
\subsection{Multi-Backbone and Multi-Dataset Validation}
\label{sec:multi_backbone}

To test whether \prism's detection generalizes beyond the main CIFAR-10
ResNet-18 setting, we re-ran the full detection and baseline protocol on
(i) CIFAR-10 with a WRN-28-10 backbone and (ii) CIFAR-100 with a
ResNet-18 backbone, using the same calibration scheme
($\alpha \in \{0.10, 0.03, 0.005\}$) and the same five seeds
($n=1000$ adversarials per attack per seed).

\begin{table}[h]
\centering
\small
\input{tables/main_attacks_multi.tex}
\caption{%
  Cross-architecture and cross-dataset detection.
  All three settings clear the L1 design FPR ($\le 0.10$);
  per-tier FPR (L2/L3) values are reported in
  Appendix~\ref{app:cifar100} and the per-seed JSONs.
  WRN-28-10 lifts CIFAR-10 TPR by $+5$--$10$~pp over ResNet-18 across
  FGSM/Square; CIFAR-100 is harder for score-based and
  optimisation-based attacks (Square $0.672$, CW $0.687$) while
  white-box PGD-40 and AutoAttack remain at $\ge 0.98$.
  The $n_{\text{adv}}$ column reports the pooled adversarial count
  across all 5 seeds and (where applicable) attack-config repeats.
  CIFAR-10/ResNet-18 and CIFAR-100/ResNet-18 use one config per seed
  ($5\times 1000 = 5000$). For WRN-28-10, FGSM/PGD pool the
  \texttt{fgsm\_pgd\_l3fix\_wrn} and \texttt{paper\_wrn} runs
  ($5+2$ seed-repeats $= 7000$), Square pools
  \texttt{prism\_square\_wrn} and \texttt{paper\_wrn}
  ($4+2 = 6000$), and AutoAttack pools the \texttt{fast\_wrn} run
  plus the \texttt{paper\_wrn} 5-seed sweep ($5+5 = 10000$); CW
  uses the canonical 5-seed run ($5000$).
}
\label{tab:main_multi}
\end{table}

\begin{table}[h]
\centering
\small
\input{tables/baselines_multi.tex}
\caption{%
  Design-FPR ($\le 10\%$) baseline comparison across all three settings
  (5 seeds, $n=1000$). The comparison is at exactly matched FPR only on
  CIFAR-10/ResNet-18; on WRN-28-10 and CIFAR-100 three baselines exceed
  the $10\%$ design target (disclosed below) and are reported unfiltered.
  \prism is the only detector that holds $\ge 0.8$ mean TPR
  on all three settings while keeping FPR below the design target;
  every baseline collapses on at least one attack in every setting
  (ODIN/Energy on PGD; LID on Square; Mahalanobis on FGSM/Square on
  CIFAR-100). The recent unsupervised detector SID
  \cite{tian2021detecting} (2021) is the weakest in every setting
  (mean TPR $0.15$--$0.40$), confirming that PRISM's margin is not an
  artifact of comparing only against pre-2021 baselines.
  \textbf{FPR-target disclosure}: in the WRN-28-10 setting,
  Mahalanobis ($0.122$) and LID ($0.105$) exceed the $\alpha{=}0.10$
  design FPR; in the CIFAR-100 setting, ODIN ($0.116$) and SID
  ($0.109$) exceed it.
  Re-calibrating these detectors at a tighter conformal $\alpha{=}0.073$
  (matched to \prism's empirical FPR) would lower their reported TPR
  further; the present table reports the published-protocol values
  unfiltered for transparency.
}
\label{tab:baselines_multi}
\end{table}

\begin{figure}[h]
  \centering
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig_baselines_cifar10.pdf}
    \caption{CIFAR-10 (WRN-28-10).}
    \label{fig:bars_c10}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig_baselines_cifar100.pdf}
    \caption{CIFAR-100 (ResNet-18).}
    \label{fig:bars_c100}
  \end{subfigure}
  \caption{Per-attack TPR at the design FPR target ($\le 0.10$; not all
  baselines hit it on WRN/CIFAR-100, see Table~\ref{tab:baselines_multi}).
  \prism (blue) is
  the only detector that holds $\ge 0.85$ on at least three of four
  attacks per dataset; ODIN/Energy collapse to $0$ on PGD; LID and
  Mahalanobis collapse on Square or FGSM. AutoAttack column has only
  \prism because baselines were not run on AutoAttack.}
  \label{fig:baselines_panel}
\end{figure}

\begin{figure}[h]
  \centering
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig_tiers_cifar10.pdf}
    \caption{CIFAR-10 (WRN-28-10).}
    \label{fig:tiers_c10}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig_tiers_cifar100.pdf}
    \caption{CIFAR-100 (ResNet-18).}
    \label{fig:tiers_c100}
  \end{subfigure}
  \caption{\prism three-tier operating points (L1, L2, L3) per attack.
  AutoAttack and PGD-40 saturate near $\mathrm{TPR}{=}1$ even at the
  tight L3 FPR (${\le}0.005$); Square trades TPR for tighter FPR most
  steeply, consistent with its score-based attack signature being the
  weakest of the four in the ensemble feature space.}
  \label{fig:tiers_panel}
\end{figure}

\paragraph{Cross-setting take-aways.}
(i)~\prism transfers to a deeper architecture (WRN-28-10) without
re-tuning: the WRN run lifts every CIFAR-10 attack TPR while holding
FPR at $0.073$, indicating that the discriminative signal is in the
ensemble channels, not in a ResNet-18-specific calibration.
(ii)~Cross-dataset to CIFAR-100 (10$\times$ more classes), white-box
gradient attacks remain trivially detectable (PGD-40 $0.984$,
AutoAttack $0.999$), but score-based Square ($0.672$) and the
optimisation-based CW-L2 ($0.687$) drop below the $0.85$ design target.
Per-channel ablation (Appendix~\ref{app:cifar100}, Table~\ref{tab:c100_abl})
shows the gap is concentrated in the StabilityV2 channel, suggesting
per-dataset retuning of stability features as a natural next step.
(iii)~Even where \prism underperforms its $0.85$ target on CIFAR-100,
it still beats the strongest baseline on every CIFAR-100 attack
(Square: $+22.7$~pp vs ODIN; FGSM: $+6.8$~pp vs ODIN).
Figure~\ref{fig:baselines_panel} visualises both points; the steeper
L3 trade-off on CIFAR-100 in Figure~\ref{fig:tiers_panel} (subfigure
\ref{fig:tiers_c100}) reflects the larger 100-class label space.

% ── Adaptive attacks ──────────────────────────────────────────────────────
\subsection{Adaptive Attack Evaluation}
\label{sec:adaptive}

Following Carlini et al.~\cite{carlini2019evaluating}, we evaluate
\prism against a white-box adaptive attacker that crafts perturbations
explicitly suppressing the detector's signals. The canonical adaptive
attack used to generate Table~\ref{tab:adaptive_pgd} is the BPDA-style
PGD launched with \texttt{run\_adaptive\_pgd.py --through-scorer},
whose loss is
\[
  \mathcal{L} = -\mathcal{L}_\text{CE}(x', y_\text{pred})
    \;+\; \lambda \sum_{\ell} \tfrac{1}{D_\ell}\|a_\ell(x') - a_\ell(x)\|_2
    \;+\; 0.5 \cdot |\mathrm{HF}_\text{DCT}(x') - \mathrm{HF}_\text{DCT}(x)|,
\]
where $a_\ell(x)$ is the per-sample clean activation at monitored layer
$\ell$ and $\mathrm{HF}_\text{DCT}$ is the differentiable
high-frequency DCT-energy proxy. The activation-matching term targets
\tamm and the DCT-energy term targets the ensemble scorer's
high-frequency channel, so \emph{two} of \prism's six raw feature
channels are attacked jointly. The remaining channels --- StabilityV2 (a
jitter-stability statistic with no closed-form differentiable
surrogate), gradient-norm, softmax-entropy, and logit-profile (and the
$190$ side-quadratic interaction terms built from these non-TDA
channels, \S\ref{sec:tamm}) --- have no dedicated suppression term in the
loss, and we explicitly return to this partial coverage in
\S\ref{sec:conclusion} and Appendix~\ref{app:cifar100}. We sweep
$\lambda \in \{0, 0.5, 1, 2, 5, 10\}$ at $100$ PGD steps with $10$ random
restarts; runs use the deterministic \prism forward pass, so EOT
averaging is verified as a no-op via $20$ resampling passes
(\texttt{eot\_verify\_samples}=20 recorded in the per-seed JSON
metadata).

\begin{table}[h]
\centering
\small
\input{tables/adaptive_pgd.tex}
\caption{%
  Adaptive PGD with activation-matching term ($\lambda$ sweep, 100 steps,
  10 restarts, 5 seeds). TPR degrades monotonically as $\lambda$ grows,
  bottoming at $0.429$ at $\lambda{=}10$ while FPR holds at the calibrated
  $0.075$. The $\lambda{=}0$ row ($0.832$) is \emph{not} a non-adaptive
  PGD reference: the fixed DCT high-frequency suppression term
  (coefficient $0.5$) is still active at $\lambda{=}0$, making this row a
  ``DCT-only adaptive'' attack. The non-adaptive PGD-40 reference is
  $0.987$ (Table~\ref{tab:main}).
}
\label{tab:adaptive_pgd}
\end{table}

\paragraph{Honest framing.} As $\lambda$ grows, an attacker who is
willing to spend perturbation budget on suppressing the TAMM
activation-matching and DCT-energy channels can substantially reduce
\prism's CIFAR-10 detection rate (TPR drops from $0.832$ at
$\lambda{=}0$ to $0.429$ at $\lambda{=}10$); FPR is unaffected because
the conformal threshold is fixed on clean calibration scores and does
not depend on the adversary's loss. On CIFAR-100 the same attack only
moves TPR from $0.950$ to $0.925$; we attribute this to the
StabilityV2 channel, which the present attack does not target and
which the ablation (Table~\ref{tab:c100_abl}) identifies as
CIFAR-100's dominant discriminative channel
(Appendix~\ref{app:cifar100}). A genuinely ensemble-complete adaptive
attack --- adding differentiable surrogates for the StabilityV2,
gradient-norm, and softmax-entropy losses --- is the most important
open evaluation gap (\S\ref{sec:conclusion}); the present sweep bounds
the achievable damage of the two-channel attacker. We do not claim
adaptive-attack robustness against an ensemble-complete attacker;
instead, we report degradation curves so deployments can choose
operating points and so future work can benchmark against a documented
baseline.

% ── Ablation ──────────────────────────────────────────────────────────────
\subsection{Ablation Study}
\label{sec:ablation}

We ablate seven configurations:
(1)~Full \prism;
(2--5)~four channel-mask arms that zero one ensemble feature family at
scoring time --- No-LogitProfile, No-StabilityV2, No-GradNorm, No-DCT ---
each isolating a distinct channel's contribution;
(6)~Ensemble-no-TDA, which removes persistence features while keeping
softmax-entropy, gradient-norm, DCT, and stability channels; and
(7)~TDA-only with its own base-Wasserstein conformal calibrator.
The channel-mask design replaces an earlier "No MoE" arm that was
statistically indistinguishable from Full \prism ($t{=}0$, $p{=}1$ across all
attacks) because the MoE router is a downstream gating module that does not
alter ensemble scores; the new arms instead surface each feature family's
attack-specific failure mode (Table~\ref{tab:ablation}). SACD/L0 is
evaluated separately in campaign streams because it is a temporal mechanism,
not an IID detector.

\begin{table}[h]
\centering
\small
\input{tables/ablation.tex}
\caption{%
  IID ablation generated from the post-fix multi-seed ablation JSON
  (5 seeds, $n=1000$ per seed). C1 is evaluated as the marginal gap between
  Full \prism and Ensemble-no-TDA (mean TPR $0.918 - 0.787 = +13.1$~pp).
  Each channel-mask arm exposes a distinct failure mode:
  No-LogitProfile FPR explodes to 0.938 (the logit channel is the
  FPR-controller); No-StabilityV2 PGD collapses to 0.396 (stability is the
  PGD signal); No-GradNorm FPR triples to 0.218 (gradient norm is a
  counter-evidence channel); No-DCT FGSM drops to 0.751 (DCT is the FGSM
  signal). All channel arms reach $p<0.001$ on at least one attack.
  \textbf{Reading guide (matching Table~\ref{tab:c100_abl}).} Rows whose
  mean FPR exceeds the $0.10$ design target --- No-LogitProfile ($0.938$),
  No-GradNorm ($0.218$), and No-StabilityV2 ($0.107$) --- are threshold
  collapses, not improved detection: their higher mean TPR is bought by
  flagging far more clean inputs and is non-deployable. Only Full \prism
  ($0.074$), No-DCT ($0.098$), and Ensemble-no-TDA ($0.077$) sit at the
  design FPR, so only their TPRs are comparable to Full \prism.
}
\label{tab:ablation}
\end{table}

\paragraph{Baseline comparison.}
Table~\ref{tab:baselines} reports four baseline detectors at the same
$\alpha{=}0.10$ design target, aggregated across the same 5 seeds. \prism
Full has both the lowest empirical FPR ($0.074$) and the highest mean TPR
($0.918$), outperforming the strongest baseline (Mahalanobis, mean TPR
$0.582$ at FPR $0.077$) by $+33.6$~pp mean TPR. PRISM is the only
detector that does not collapse on at least one attack family
(ODIN/Energy: PGD TPR $\approx 0.001$; LID: Square TPR $0.170$).
Energy's empirical FPR ($0.105$) modestly exceeds its $\alpha{=}0.10$
design target on this test split, which we report unfiltered; recomputing
at a tighter conformal $\alpha{=}0.075$ (matched to \prism's empirical
FPR) would lower Energy's mean TPR further.
We additionally benchmark two recent (2021) detectors---SID
\cite{tian2021detecting} and the supervised SpectralDefense
\cite{harder2021spectraldefense}---in Appendix~\ref{app:recent_baselines}.
Both underperform \prism: SID across all attack families, and
SpectralDefense everywhere except the gradient-$L_\infty$ attacks it is
trained on, collapsing on black-box (Square) and $L_2$ (CW) attacks even
in-distribution (Table~\ref{tab:recent_baselines}) and failing to transfer
across attack families (Table~\ref{tab:spectral_cross}).

\begin{table}[h]
\centering
\small
\input{tables/baselines.tex}
\caption{%
  Matched-FPR baseline comparison (5 seeds, $n=1000$ per seed).
  \prism Full beats the best baseline by $+33.6$~pp mean TPR while holding
  the lowest FPR.
}
\label{tab:baselines}
\end{table}

\paragraph{Temporal and recovery contributions.}
SACD/L0 is reported from campaign-stream experiments (\S\ref{sec:campaign})
and TAMSH from L3 recovery experiments (\S\ref{sec:recovery}). Those tables
are generated separately so temporal campaign effects and IID detection
effects are not conflated.

% ── Score distribution ────────────────────────────────────────────────────
\subsection{Score Distribution Analysis}
\label{sec:scored}

\paragraph{Reporting.}
Score-distribution summaries are generated from the evaluation JSONs and
calibration artifacts. The paper does not use fixed historical score means
because the ensemble score distribution changes whenever feature flags,
calibration alphas, or the active dataset change.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.75\linewidth]{figures/fig2_score_dist.pdf}
  \caption{Score distributions for clean and adversarial inputs
           (CIFAR-10, ResNet-18, single-attack reference).
           Dashed lines: conformal thresholds L1, L2, L3.
           Clean and PGD-40 distributions are clearly separated;
           FGSM at low $\varepsilon$ overlaps the clean tail.}
  \label{fig:score_dist}
\end{figure}

\begin{figure}[h]
  \centering
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig_score_dist_cifar10.pdf}
    \caption{CIFAR-10 (WRN-28-10).}
    \label{fig:sd_c10}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig_score_dist_cifar100.pdf}
    \caption{CIFAR-100 (ResNet-18).}
    \label{fig:sd_c100}
  \end{subfigure}
  \caption{Per-attack score envelopes (p5--p95 whiskers, IQR boxes,
  medians) of the fused ensemble score $S(x)$ for clean (blue) vs
  adversarial (red) inputs across all four attacks per dataset. $S(x)$
  is signed: clean inputs centre near zero (CIFAR-10 clean median
  $\approx 0.3$) and attacks push it positive. AutoAttack pushes scores
  furthest from the clean median (CIFAR-10 adversarial median
  $\approx 17$); Square has the tightest separation, matching the lower
  Square TPR in Table~\ref{tab:main_multi}. CIFAR-100 (right) shows the
  same ordering with a slightly larger clean spread (clean median
  $\approx 0.04$, p5--p95 width $5.9$ vs $4.1$ on CIFAR-10), reflecting
  the 100-class softmax temperature. This is the same $S(x)$ scale as
  the per-attack histograms in Appendix~\ref{app:score_hist}
  (clean mean $0.27$).}
  \label{fig:score_dist_panel}
\end{figure}

% ── Campaign detection ───────────────────────────────────────────────────
\subsection{Campaign Detection (SACD): Latency, Not ASR Gap}
\label{sec:campaign}

We evaluate L0 campaign detection on clean-only, sustained ($\rho \in
\{0.5, 0.8, 1.0\}$), low-rate ($\rho{=}0.05$), and burst stream scenarios
generated by \texttt{run\_campaign\_eval.py}.

\paragraph{Headline metric: time-to-detect.}
On sustained $\rho{=}1.0$ streams, the L0 monitor flags the campaign within
a mean of $\mathbf{2.6 \pm 0.5}$ queries after the first adversarial step
(across 5 seeds; Table~\ref{tab:campaign}). On $\rho{=}0.5$ streams,
detection takes $3.4 \pm 0.9$ queries; on 10-step burst streams,
$12.0 \pm 0.0$ queries; on low-rate $\rho{=}0.05$ streams,
$17.4 \pm 8.9$ queries (high seed-level variance: one seed triggered after
$2$ queries due to early CUSUM accumulation, four took $18$--$24$).

\begin{table}[h]
\centering
\small
\input{tables/campaign.tex}
\caption{%
  Campaign detection across stream scenarios (5 seeds, post-fix). The
  headline metric is mean time-to-detect; clean-only L0 activation fraction
  is 0.000 on all 5 seeds after the F1 CUSUM drift-floor fix.
}
\label{tab:campaign}
\end{table}

\paragraph{Why time-to-detect, not ASR gap.}
A naive headline metric ---
adversarial success rate (ASR) reduction with L0 on vs.\ off at
$\rho{=}1.0$ --- is structurally bounded by the upstream L3 conformal
threshold, which already rejects approximately 99\% of $\rho{=}1.0$
adversarials within 22 queries, leaving no room for L0 to contribute an
additional ASR-reduction gap. We therefore report ASR gap in the appendix
for completeness, but treat detection latency (time-to-first-trigger after
campaign onset) as the operationally meaningful metric: a defender that
learns of an attack within 3 queries can deploy mitigations
(rate-limiting, account quarantine, model rollback) before the attacker
has issued more than a handful of queries against the deployed system.

\paragraph{Clean-only false alarm rate.}
After the F1 CUSUM drift-floor fix, L0 activation fraction on the
500-step calibration clean streams is $0.000$ on all 5 seeds (pre-fix:
up to $0.594$ on 2 of 5 seeds, a defect that would not have survived
peer review pre-fix). For 50-query clean windows (warmup$=20$, 30
at-risk steps), empirical false alarm probability is $< 5\%$.
A stress test on longer clean streams (bootstrap-resampled from
$n{=}1{,}000$ real post-fusion ensemble scores generated end-to-end on
the VAL split) confirms that the deployed $C^*=5.0$ holds the
$0.000$ active-fraction through $1{,}000$ steps and starts admitting
its first spurious trigger between step $\approx 1{,}800$ and step
$\approx 4{,}500$ on real long streams
(Appendix~\ref{app:l0_stress}, Table~\ref{tab:l0_stress}). The
alternative feasible cells $C^* \in \{8.0, 10.0, 12.0\}$ from the
calibration grid hold $0.000$ across all stream lengths up to
$10{,}000$ steps. Longer-window deployments should either pick one of
these conservative cells or periodically reset L0 / auto-deactivate on
return to the clean score baseline.

\paragraph{Two distinct false-alarm rates.}
The \emph{FPR (clean steps)} column in Table~\ref{tab:campaign}
($0.084$--$0.143$) is the per-query L1/L2/L3 conformal alert rate of the
underlying IID detector --- the same FPR reported in
Table~\ref{tab:main} --- not the L0 campaign-trigger rate. A deployment
that quarantines accounts should gate on L0 firing (clean-only rate
$0.000$ after the F1 fix), not on every L1 step; L1 alerts are
appropriate for softer signals such as elevated logging or rate-limiting,
where a $\sim$10\% per-query rate is acceptable because the action is
reversible and per-query.

% ── Recovery (TAMSH) ─────────────────────────────────────────────────────
\subsection{L3 Recovery (TAMSH)}
\label{sec:recovery}

We measure recovery accuracy of five routing strategies applied to the
subset of PGD inputs that trigger the L3 (reject) tier: the most aggressive
adversarials, where simply rejecting the input is the conservative default.
We pre-registered a $+15$~pp recovery gap (best learned router vs.\
passthrough) as the contribution target for this component. As reported
below, the \emph{learned} router (combined $H_0{+}H_1$ Wasserstein gating)
falls $4.4$~pp short of that target at $+10.6$~pp; we therefore report this
component as a positive-signal contribution rather than a target-clearing
result, and document the residual gap to the prior-augmented variant and
the oracle ceiling as the path for future learned routing.

\begin{table}[h]
\centering
\small
\input{tables/recovery.tex}
\caption{%
  L3-recovery on PGD adversarials (5 seeds, $n_{L3} \approx 792$ per
  seed; mean trigger rate $0.792$ within sanity band $[0.10, 0.82]$). The
  learned-router row (\texttt{tamsh}, combined $H_0{+}H_1$) is the
  primary contribution and delivers $+10.6$~pp over passthrough, falling
  $4.4$~pp short of the pre-registered $+15$~pp target. The
  \texttt{tamsh\_force} row is a prior-augmented upper-bound analysis
  (always-route to PGD specialist, not a learned policy); the oracle
  ceiling is per-input best-of-$K$ on the same L3 subset.
}
\label{tab:recovery}
\end{table}

\paragraph{Router contribution isolation.}
The \texttt{tamsh\_uniform} arm replaces topology-distance gating with
uniform $1/K$ mixing across all 4 experts. Its recovery accuracy equals
that of passthrough ($0.033$ each, paired gap $0.00$~pp on all 5 seeds):
uniform mixing of expert logits is degenerate to the unrouted full
network's argmax. The $+10.6$~pp \texttt{tamsh} gap (combined $H_0{+}H_1$
Wasserstein gating) over uniform is therefore attributable entirely to the
routing signal, not to standalone expert-network capacity --- a positive
but partial result against the pre-registered $+15$~pp target. The further
$+15.4$~pp lift achieved by \texttt{tamsh\_force} (always-route to the
PGD-trained expert) reflects correct routing under a strong prior: L3-
rejected inputs are by definition high-confidence adversarials, and PGD
is the dominant adversarial signal in our training mix. We treat
\texttt{tamsh\_force} as an upper-bound analysis rather than a deployable
policy, since it bakes in an oracle assumption about attack identity that
a real router would have to learn online. The gap between the learned
router ($0.139$ recovery accuracy) and the prior-augmented variant
($0.293$) bounds the headroom achievable by a stronger gating signal
below the oracle ceiling ($0.399$).

% ── Calibration ──────────────────────────────────────────────────────────
\subsection{Calibration Sensitivity}
\label{sec:calibration}

Figure~\ref{fig:calib} sweeps $n_\text{cal} \in \{500, 1000, 2000, 3000\}$
and confirms threshold stability: the L3 threshold varies by $\pm 0.03$
(range $0.06$) across calibration set sizes (25 bootstrap subsamples per
size), and the achieved FPR on the held-out validation split tracks the
target $\alpha$ within the Wilson-95\% interval, satisfying
Proposition~\ref{prop:coverage}.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.75\linewidth]{figures/fig3_calibration.pdf}
  \caption{Conformal threshold stability vs.\ calibration set size
           $n_\text{cal}$. \emph{Left}: L1/L2/L3 threshold values (mean
           $\pm$ 95\% bootstrap envelope; L3 half-range $\pm 0.03$ across
           the full sweep). \emph{Right}: Achieved FPR on the held-out
           validation set tracks the target $\alpha$ for all three tiers.
           Source: \texttt{clean\_scores.npy} (2{,}000 calibration scores)
           with 25 bootstrap subsamples per size.}
  \label{fig:calib}
\end{figure}
